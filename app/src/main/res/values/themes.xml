<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Locator360" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorSecondaryVariant">@color/orange</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->

        <!-- enable window content transitions -->
        <item name="android:windowActivityTransitions">true</item>

<!--        &lt;!&ndash; specify enter and exit transitions &ndash;&gt;-->
        <item name="android:windowEnterTransition">@android:transition/explode</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@android:color/transparent</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>

    </style>
    
    <style name="Style_Round_Dialog_Corner" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:windowBackground">@drawable/round_dialog_corners</item>
        <item name="android:windowMinWidthMinor">80%</item>

    </style>

    <style name="Search_View" parent="Widget.AppCompat.SearchView">

         Query Text color
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Changing the icon tint, hint icon tint, and remove text (cross) tint-->
        <item name="android:textColorSecondary">@color/white</item>
        <!-- Query hint -->
        <item name="queryHint">Search Contact</item>
        <!-- Query hint color -->
        <item name="android:textColorHint">@color/white</item>

    </style>

    <style name="Progress_bar" parent="Widget.AppCompat.ProgressBar">
        <item name="colorControlActivated">@color/white</item>
    </style>

    <!-- Animation for profile image dialog preview -->
    <style name="SlidingDialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

    <style name="Member.Status.Style" parent="">
        <item name="shapeAppearance">@style/ShapeAppearanceOverlay.App.circleImageView</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.circleImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="Round.Corners" parent="">
        <item name="shapeAppearance">@style/corners</item>
    </style>

    <style name="corners" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20%</item>
    </style>

    <!--TabLayout-->
    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyCustomTextAppearance</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
    </style>

    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@color/orange</item>
    </style>

    <style name="Theme.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/orange</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">250</item>
        <item name="postSplashScreenTheme">@style/Theme.Locator360</item>

    </style>
</resources>